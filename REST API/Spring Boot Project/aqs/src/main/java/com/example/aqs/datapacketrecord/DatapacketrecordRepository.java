package com.example.aqs.datapacketrecord;

import org.springframework.data.jpa.repository.JpaRepository;

public interface DatapacketrecordRepository extends JpaRepository<Datapacketrecord, Long> {
}
